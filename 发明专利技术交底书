发明专利技术交底书
—— 一种多级整数分离存审计计算方法
项目	内容
发明名称	多级整数分离定点算术引擎及跨语言可审计计算方法
技术领域 (建议分类)	G06F 7/483；G06F 7/57；H04L 9/06；G06F 21/64
发明人	我
交底日期	2026-09-03
技术层级	计算机算术（定点十进制 ALU 上层软件实现）+ 密码学证据链
优先权策略	

【创新点 3】按运算类型分层调度（累积 vs 不累积硬边界）finalize（封存）	作为 R1 舍入的影子：决定 K 边界是否 +1 ULP；register 本身舍入后可转负值（超计审计）	REGISTER_CARRY 不是舍入模式；真正的舍入只在 finalize 时做一次，避免中间结果在 K 边界上反复抖动。	T2（HALF_UP 最优性）、P3（幂等）

【创新点 4】6 种显式舍入模式 × R0/R1过

R0/R1 双模式解耦:

                       → 用户对某一步临时需要保守/激进舍入时, 不影响整条链的真值锚点

属性保护三原则 (__setattr__ 保证):
  1. precision_k 不可变
  
与现有技术区别：Decimal/BigDecimal 的精度/舍入是全局 Context，线程 A 调 
【创新点 5】5A 负号绝对值方案（寄存器全程非负不变式）
5A 方案 — 符号-数值分离:

  乘法 / 除法 / 开方: 进入整数运算前
      sign =      # 符号独立
      |a|, |b| = abs 运算


压力场景	本发明寄存器状态	若不做 5A 方案
S1：20 步纯乘，全正符号	register ∈ [0, 10^32)	需 Z3 S1a 证明无溢出
S3：10 步乘除
precision步 / S4 归纳	11	创新点5（5A 封闭，S4a 归纳步）
合计	—	73	—

单条声明验证方法（73 条均相同）:
  对声明 P, 构造 ¬P 谓词 → Z3 SMT 求解器 QF_LIA 理论

3.3 有益效果（定量实验数据支撑）
测试维度	测试场景	QinMath	float64	优势倍数
加法长链精度	10^6 步链式加法	≈10^ 优 vs float

四、现有技术对比表（新颖性/创造性切入点）
技术特征	本发明 无	❌ 每步截断

五、具体实施例（覆盖 Python / Java / C++ 三语言，构造多语言护城河）
实施例 1：Python 参考实现（核心骨架）
class QinNumber:
    # 创新点1：三级整数存储
    integer_part : int            # L0 无界
 expected_hash

}
实施例 3：C++ 移植骨架（极端性能场景）
// qin_number.hpp  ——  GMP mpz_class 后端
class Q    // 10^32 << 2^64, 可直接用 uint64_t
 
};
六、附图及说明（建议 4 张图）

八、常见规避方案与专利应对
规避尝试	效果评估	应对策略 / 权项覆盖点
把 k=32 改成 k=28 或 值的第二层寄存器」，从属写 k=32 具体；说明书写满 k 的广义实施。

九、专利申请与交付建议
（1）申请方式：nAvalanche Guard 演示前端，不会泄漏核心算法细节。

（5）发明人：Qin Chen。



交底书结束 · 发明名称：多级整数分离定点算术引擎及跨语言可审计计算方法
发明人：我 · 2026-09-03
